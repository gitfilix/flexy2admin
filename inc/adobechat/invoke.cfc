component
{
	public function getLoginTime()
	{
	uuid = CreateUUID();
	thread
	action="run"
	name=uuid
	{
	    i=0;
		while(i < 100 ){//running  for 100 sec only
			sleep(1000);
			i = i +1;
			WsSendMessage(i);
	         	}
	         }
	}
	
}